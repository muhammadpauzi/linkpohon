<div class="max-w-2xl mx-auto py-5">
    <div class="text-center mx-auto p-3 py-10 w-full">
        <div class="mb-10">
            <h1 class="text-5xl font-bold text-purple-500 mb-4 leading-normal">One LinkPohon For All Your Links and
                Needs</h1>
            <p class="max-w-lg mx-auto text-center text-gray-500 font-medium leading-relaxed mb-10 ">Share your
                social
                media profiles,
                online
                stores,
                works and all your links with just one LinkPohon</p>
            {{#if user.isAuthenticated}}
            <a href="/{{ user.username }}"
                class="bg-purple-500 text-white text-lg font-semibold py-4 focus:ring-2 ring-purple-500 ring-offset-2 px-7 rounded hover:shadow-lg transition">See
                My Links</a>
            {{else}}
            <a href="/sign-in"
                class="bg-purple-500 text-white text-lg font-semibold py-4 focus:ring-2 ring-purple-500 ring-offset-2 px-7 rounded hover:shadow-lg transition">Sign
                in</a>
            {{/if}}
        </div>
    </div>
</div>